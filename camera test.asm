; Serveur (camera_server.asm)
section .data
  ; ...

section .bss
  ; ...

section .text
  global _start

_start:
  ; ... (code pour la capture vidéo avec OpenCV et la communication réseau)

; Client (camera_client.asm)
section .data
  ; ...

section .bss
  ; ...

section .text
  global _start

_start:
  ; ... (code pour la réception des images et l'affichage avec OpenCV)
