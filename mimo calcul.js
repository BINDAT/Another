function calcul(height,weight) //fonction qui calcule l'IMC
{
return weight/(height * height); //calcul de l'IMC
}
const result = calcul(1.7,70); //calcul de l'IMC
console.log(result); //affichage de l'IMC

/*
calcul qui est ensuite remis dans la console log
*/