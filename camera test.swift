// Serveur (CameraServer.swift)
import AVFoundation
import Cocoa
import Network

class CameraServer: NSObject, AVCaptureVideoDataOutputSampleBufferDelegate {
    // ... (code pour la capture vidéo avec AVFoundation et la communication réseau)
}

// Client (CameraClient.swift)
import Cocoa
import Network

class CameraClient: NSObject {
    // ... (code pour la réception des images et l'affichage)
}
